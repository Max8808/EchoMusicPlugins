# 滚动助手

为 EchoMusic 的长页面补充“到底部”按钮。

## 功能

- 在当前页面还没有滚到底部时显示“到底部”按钮。
- 点击后平滑滚动到当前页面底部。
- 如果宿主的“回到顶部”按钮正在显示，会自动上移避让。
- 提供插件设置，可开关按钮、调整底部隐藏阈值和避让行为。

插件依赖 EchoMusic `>=2.2.6-beta.21`，因为它使用宿主提供的稳定滚动容器 API `ctx.scroll`。
